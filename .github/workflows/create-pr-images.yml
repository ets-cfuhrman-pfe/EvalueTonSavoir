name: create-pr-images

on:
  pull_request:
    branches:
      - recover-319
  push:
    branches:
      - recover-319

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata for Frontend Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}-pr-frontend
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
      - name: Debug - List directory structure
        run: |
          echo "=== Current directory ==="
          pwd
          echo "=== Project root contents ==="
          ls -la
          echo "=== Server directory contents ==="
          ls -la server/ || echo "server/ directory not found"
          echo "=== Looking for server/shared ==="
          ls -la server/shared/ || echo "server/shared/ directory not found"
      - name: Build and push Frontend Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./
          file: ./client/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
