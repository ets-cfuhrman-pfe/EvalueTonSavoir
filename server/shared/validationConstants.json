{
  "room": {
    "name": {
      "minLength": 1,
      "maxLength": 25,
      "pattern": "^[a-zA-Z0-9\\-_\\s]+$",
      "errorMessage": "Le nom de la salle ne peut contenir que des lettres, chiffres, tirets, underscores et espaces"
    },
    "description": {
      "maxLength": 500,
      "errorMessage": "La description ne peut pas dépasser 500 caractères"
    },
    "password": {
      "minLength": 4,
      "maxLength": 25,
      "errorMessage": "Le mot de passe doit contenir entre 4 et 50 caractères"
    },
    "maxParticipants": {
      "min": 1,
      "max": 60,
      "integer": true,
      "errorMessage": "Le nombre maximum de participants doit être entre 1 et 60"
    }
  },
  "user": {
    "email": {
      "minLength": 3,
      "maxLength": 64,
      "pattern": "^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$",
      "errorMessage": "L'adresse email doit être valide"
    },
    "username": {
      "minLength": 2,
      "maxLength": 25,
      "pattern": "^[a-zA-Z0-9,\\s]+$",
      "errorMessage": "Le nom d'utilisateur ne peut contenir que des lettres, des chiffres, des virgules et des espaces"
    }
  },
  "quiz": {
    "title": {
      "minLength": 1,
      "maxLength": 64,
      "errorMessage": "Le titre du quiz doit contenir entre 1 et 64 caractères"
    },
    "content": {
      "minLength": 1,
      "maxLength": 50000,
      "errorMessage": "Le contenu du quiz doit contenir entre 1 et 50000 caractères"
    }
  },
  "folder": {
    "title": {
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[a-zA-Z0-9\\u00C0-\\u017F\\s\\-_]+$",
      "errorMessage": "Le titre ne peut contenir que des lettres, chiffres, espaces, tirets et underscores"
    }
  },
  "text": {
    "short": {
      "maxLength": 500,
      "errorMessage": "Le texte ne peut pas dépasser 500 caractères"
    },
    "long": {
      "maxLength": 5000,
      "errorMessage": "Le texte ne peut pas dépasser 5000 caractères"
    }
  },
  "numerical": {
    "answer": {
      "maxLength": 20,
      "pattern": "^[+\\-]?\\d*\\.?\\d*([eE][+\\-]?\\d{1,2})?$",
      "errorMessage": "Cette valeur ne peut contenir que des chiffres",
      "maxLengthErrorMessage": "La réponse ne peut pas dépasser 20 caractères"
    }
  }
}